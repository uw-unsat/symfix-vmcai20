#lang racket/base

(provide metrics)

(define metrics '(term-count unused-terms union-size merge-cases))
;; available metrics
;; - time
;; - term-count
;; - unused-terms
;; - union-size
;; - merge-cases
